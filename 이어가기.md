# 이어가기

다른 기기·다른 세션에서 이 저장소를 이어받을 때 읽는다.

```bash
git clone https://github.com/Finerestaurant/primary-sources-archive.git
cd primary-sources-archive
python3 열람기/build_site.py site.json --out _site
python3 -m http.server -d _site 8000
```

파이썬 3.12 표준 라이브러리만 쓴다. 설치할 것이 없다.
`main` 에 밀면 GitHub Actions 가 같은 명령을 돌려 Pages 에 올린다.

---

## 지금 어디까지 왔나

**공개돼 있다** → https://finerestaurant.github.io/primary-sources-archive/

문서철 15개 · 1,200건.

```
조선을 밖에서 보다 154건 완료  1945.2 – 1948.12   ← 새로 넣었다
전쟁의 세 해      175건 완료   1950.6.25 – 1953.7.27
전쟁이 오기까지    152건 완료   1948.1.24 – 1950.6.23
주제 7개       in due course · 조선 · 중공군은 오는가
              일본의 항복 · 독일의 항복 · 쓰이지 않은 문서 · 진주만
국면 2개       2차대전 · 한국전쟁
```

## 하다 만 것 — CREST 1949–50 (여기부터 이어라)

**원문 156건은 다 받아 두었고 번역이 18건에서 끊겼다.** 세션 한도에 걸렸다.
잃은 것은 없다. 남은 138건만 옮기면 된다.

```
CREST_1945/raw/Q01.txt … Q156.txt      원문. 다 있다
CREST_1945/raw/index.json              색인에 들어 있다
CREST_1945/tr/Q*.json                  18건 완료
CREST_1945/pipeline/남은묶음.json       남은 138건을 24묶음으로 나눠 둔 것
CREST_1945/pipeline/에이전트_지시.md    에이전트에게 그대로 주는 글
```

**돌리는 법.** 묶음 하나에 에이전트 하나다. 한 번에 열 개 남짓이 알맞다.

> 먼저 `CREST_1945/pipeline/에이전트_지시.md` 를 읽고 그 지시를 그대로 따라라.
> 그 안의 `__IDS__` 자리에 들어갈 것은 **Q06 Q07 Q08 Q09 Q10 Q11** 여섯 건이다.

**아직 안 한 것.** 번역이 끝나면 이 셋을 해야 한다.

1. `collection.json` 의 `groups` 에 `{"key":"E","label":"1949–50 개전 전"}` 을 넣는다
   (`make_docs.py` 의 `GROUP` 에는 이미 넣어 두었다)
2. `subtitle`·`site.json` 의 `n` 과 `period`, `README.md` 의 표와 총계,
   `RIGHTS.md` 의 기간을 310건 · 1945.2–1950.6 으로 고친다
3. 다시 짓고 커밋·push

**아직 site 를 다시 짓지 않았다.** 공개된 문서철은 154건 그대로다.
1949–50년이 다 될 때까지 그렇게 둔다.

### 왜 1950년 6월 25일에서 끊는가

이 문서철은 **미국이 조선을 밖에서 보는 기록**이다. 6월 25일부터 미국은 조선
안에 있다. CREST 1950년치 494건 가운데 개전 뒤가 380건 **180만 자**인데, 지금까지
만든 것을 전부 합친 것보다 많다. 섞으면 문서철이 전쟁 기록에 파묻힌다.

**「전쟁의 세 해」(FRUS 국무부 계통) 옆에 놓일 별개의 문서철**로 보고 남겨 두었다.
뽑는 법은 이 문서철과 같다.

## 앞서 끝낸 것 — CREST 1945–48

**CIA 공개 기록(CREST)에서 고른 154건.** 네 계열이다.

```
L01–L07   1945 뉴욕 · OSS 「외국인 전문가 조사」
M01–M19   1946 만주·북조선 · 현장 첩보보고 (M04 는 뺐다. 아래 참조)
N01–N68   1947 북조선 · 중앙정보단·중앙정보국 보고
P01–P63   1948 두 정부 · 중앙정보국 보고 (P08·P51 은 사본 중복이라 뺐다)
```

**줄기가 하나라 한 문서철에 둔다.** 미국은 조선 안에 들어가지 못한 채 밖에서
읽는다. 1945년에는 뉴욕에서 사람을 만나, 1946년에는 만주에서 국경 너머로,
1947년에는 38선을 넘어온 사람에게서. 1948년에 두 정부가 서는데도 그대로다.

**주제(threads)에는 넣지 않았다.** 문서철로만 올렸다.

### 번역은 서브에이전트로 한다

**`pi` CLI 를 쓰지 않는다.** 앞선 문서철(`KOREA_1950_53`, `NDL_점령기자료`)의
`translate.py` 는 `pi` 를 부르는 껍데기인데, 이 문서철부터는 서브에이전트에
맡겼다. 자세한 이유는 `CREST_1945/pipeline/README.md`.

에이전트 하나에 문서 다섯에서 일곱 건을 주고 이렇게 시켰다. 넷을 나란히 돌려
스무 건에 열댓 분 걸렸다.

> `CREST_1945/pipeline/TRANSLATION_SPEC.md` 와
> `KOREA_1948_50/pipeline/TRANSLATION_SPEC.md` 와 `열람기/STYLE.md` 를 먼저 읽어라.
> `CREST_1945/raw/L01.txt` 를 읽고 `CREST_1945/tr/L01.json` 을 써라.
> 전문으로 옮기고, 줄이지 마라. OCR 을 교정한 영문을 `en` 에, 번역을 `ko` 에 넣어라.
> **문서번호와 날짜의 숫자는 고치지 마라.** `(b)(6)`·`25X1` 같은 삭제 표시를
> 지우지 말고 `redactions` 에도 모아라. 읽을 수 없으면 `[판독 불가]`.

**받은 것은 반드시 검증한다.** 실제로 걸린 것들이다.

- 긴 문서를 나눠 쓰다 만 이음표(`@@KO@@`)가 본문 끝에 남는다
- `ko` 가 `en` 의 절반에 못 미치면 잘렸거나 요약한 것이다. 열어 봐라
- 스키마 16칸에 여분 칸이 붙거나 빠진다

```python
# raw 대비 en·ko 길이와 스키마를 한꺼번에 본다
python3 - <<'EOF'
import json,glob,os
SPEC=['doc_id','date','doc_type','from','to','classification','title_en','title_ko',
      'subject_ko','key_points_ko','en','ko','redactions','people','confidence','notes']
raw={os.path.basename(p)[:-4]:len(open(p).read()) for p in glob.glob('CREST_1945/raw/*.txt')}
for p in sorted(glob.glob('CREST_1945/tr/*.json')):
    d=json.load(open(p)); i=d['doc_id']; r=raw.get(i,0)
    en,ko=len(d.get('en') or ''),len(d.get('ko') or '')
    bad=[k for k in SPEC if k not in d]+[k for k in d if k not in SPEC]
    print(i,r,en,ko,'짧다' if en<r*0.6 else '', bad or '')
EOF
```

### 사본 중복을 먼저 걸러라

**CREST 는 부서마다 남은 사본을 따로 디지털화한다.** 그래서 한 간행물이 서로
다른 문서번호로 두 벌 올라와 있다. 1948년치에서 세 쌍이 나왔다. 날짜와 제목이
달라 보여도 같은 것일 수 있다 — `ORE 15-48` 은 메타데이터 날짜가 한 달 반
벌어져 있었다.

**옮기기 전에 걸러라.** 다 옮기고 나서 알면 그 품이 버려진다.

```python
def shingles(t, k=8):
    w = re.findall(r'[a-z]+', t.lower())
    return {' '.join(w[i:i+k]) for i in range(len(w) - k)}
len(A & B) / len(A)      # 0.7 을 넘으면 같은 문서로 보고 열어 봐라
```

사본마다 읽히는 데가 다르다. **더 온전한 쪽을 싣고 다른 쪽의 문서번호를
`notes` 에 적는다.**

### 긴 문서는 갈라 써라

**`N01`(2만 5천 자)에서 에이전트가 두 번 중단됐다.** 「Output blocked by content
filtering policy」다. 영문 전문을 통째로 다시 뱉는 대목에서 걸린다. 1만 자
아래는 걸리지 않았고 2만 자 위에서 걸렸다.

그래서 이 한 건만 **영문과 한국어를 갈라 만들었다.**

```python
# en 은 기계로. 줄 끝 하이픈으로 잘린 낱말을 잇고 문단을 되돌린다
import sys, re; sys.path.insert(0,'열람기'); from reflow import reflow
raw = open('CREST_1945/raw/N01.txt').read()
en = reflow(re.sub(r'(?<=[a-z])-[ \t]*\n[ \t]*(?=[a-z])', '', raw))
```

그렇게 `en` 을 채운 뼈대 JSON 을 먼저 만들고, 에이전트에게는 **한국어만** 맡겼다.
번역은 옮기는 일이라 걸리지 않는다. 손으로 교정하지 않았다는 사실은 그 문서의
`notes` 에 적어 두었다.

도구로 만들어 두었다 — `CREST_1945/pipeline/mkskel.py`.

    python3 CREST_1945/pipeline/mkskel.py P01 P12 P43

**13만 자를 넘으면 그마저 안 된다.** `P01`(SR-2, 영문 137,000자)은 영문을
조각으로 갈라 다섯 에이전트가 나눠 옮기고 이어 붙였다. 조각마다 같은 말을
쓰도록 **용어표를 적어 함께 주었다.** 말이 갈리면 한 문서가 아니게 된다.

이 방법은 **OCR 이 깨끗할 때만 쓴다.** `N01` 은 CIA 가 `RELEASE IN FULL` 로 낸
것이라 스캔이 좋았다. 뭉갠 문서에 쓰면 교정 안 된 영문이 그대로 올라간다.

### 날짜를 믿지 마라

**M04 를 뺐다.** 메타데이터가 `1946-05-10` 인데 문서 표제가
「WEEK OF 4 MAY - 10 MAY 1948」이었다. **월과 일은 맞고 연도만 두 해 어긋난다.**
번역은 끝나 있고 `_staging/CREST_1948_ORE/` 에 옮겨 두었다.

새 CREST 문서철을 지을 때는 **본문에서 연도를 뽑아 메타데이터와 맞춰 보고,
메타 연도가 본문에 한 번도 안 나오면 열어 본다.** 다만 정보보고의 `DIST.` 날짜가
이듬해인 것은 어긋난 것이 아니다 — 정보를 얻은 날과 배포한 날이 다르다.

**어긋난 것이 세 번 다 판단서(ORE)였다.** 현장 첩보보고는 메타데이터가 맞는데
완성된 분석문은 표지 날짜를 봐야 한다. `N44`(ORE 62)는 메타 10월 2일에 표지
**11월 18일**이라, 그대로 두었으면 11월 5일 유엔 총회 표결을 논하는 문서가
표결보다 앞선 것이 될 뻔했다. `ORE 15-48` 은 메타 2월 4일에 표지 3월 18일이다.
판단서는 표지가 있으니 반드시 열어 봐라. 현장 첩보보고에서는 한 번도 틀리지 않았다.

### 다시 지을 때

```bash
python3 CREST_1945/pipeline/make_docs.py
cd CREST_1945/reading && python3 ../../열람기/build.py collection.json reader.html && cd ../..
python3 열람기/make_people.py
python3 열람기/build_site.py site.json --out _site
```

---

## 그 밖에 남은 것

- 「38선을 넘을 것인가」·「이승만을 어떻게 할 것인가」 주제 — 1951–53년 문서가
  들어왔으니 이제 만들 수 있다. 기준은 `조사/README.md` 가 아니라 아래 원칙
- **JANIS 75** — 1945년 조선 정보 연구 386만 자. 넣을지 말지 정해야 한다
- **CREST 1949년 이후** — 1949년치부터 이어 가면 된다. 뽑는 법은 위와 같다
- **`_staging/CREST_1948_ORE/`** — 1948년 5월 10일자 주간 요약 한 건.
  선거 엿새 전 문서인데 여러 나라가 한 장에 들어 있어 이 문서철에 안 넣었다
- **`_staging/CIG_1946_일보/`** — 1946년 정보 일보 네 건. 같은 이유로 뺐다
- **`_site` 가 721MB 다.** GitHub Pages 산출물 한도가 1GB 다. 스캔을 PNG 에서
  JPEG 으로 바꾸면 크게 준다

## 주제에 문서를 고르는 기준

기계로 고르지 않는다. 시험은 하나다.

> **이 줄을 빼면 다음 줄이 이해되지 않는가.** 이해되면 뺀다. 중요해도 뺀다.

- **문장 시험** — 한 줄을 써 본다. 제목을 바꿔 적은 것이면 넣지 않는다
- **어긋남 우선** — 같은 것을 두 곳이 다르게 적은 자리는 무조건 넣는다
- **틀린 판단을 빼지 않는다** — 결과를 아는 쪽에서 골라내면 사후의 정답만 남는다
- 주제 하나는 15~30항. 넘치면 물음이 둘인 것이다
- 그날짜 문서가 없으면 **걸지 않는다.** 점선으로 둔다

## 조사 기록

`조사/README.md` 에 무엇이 어디 있고 어디가 막혔는지 적어 두었다.
**같은 곳을 두 번 뒤지지 않으려고 둔 것이다.** 새 세션은 그것부터 읽어라.
