# 이어가기

다른 기기·다른 세션에서 이 저장소를 이어받을 때 읽는다.

```bash
git clone https://github.com/Finerestaurant/primary-sources-archive.git
cd primary-sources-archive
python3 열람기/build_site.py site.json --out _site
python3 -m http.server -d _site 8000
```

파이썬 3.12 표준 라이브러리만 쓴다. 설치할 것이 없다.
`main` 에 밀면 GitHub Actions 가 같은 명령을 돌려 Pages 에 올린다.

---

## 지금 어디까지 왔나

**공개돼 있다** → https://finerestaurant.github.io/primary-sources-archive/

문서철 16개 · 1,732건. `_site` 850MB (한도 1GB).

```
전쟁을 보는 눈    822/823건   1950.6.26 – 1951.12.29   ← 2026.8.8 1951년치 실질 완료(장문 4건 포함)
조선을 밖에서 보다 309건 완료  1945.2 – 1950.6.22
전쟁의 세 해      175건 완료   1950.6.25 – 1953.7.27
전쟁이 오기까지    152건 완료   1948.1.24 – 1950.6.23
주제 7개       in due course · 조선 · 중공군은 오는가
              일본의 항복 · 독일의 항복 · 쓰이지 않은 문서 · 진주만
국면 2개       2차대전 · 한국전쟁
```

**2026.8.9**: 새 문서철 착수 — **`JACAR_독단전행`**(아직 사이트에 안 걸림, 번역
0건). 사용자가 "관동군이 대본영 몰래 확전한 사건이 노구교 말고도 더 있지 않냐"고
물어서 조사하다 여섯 사건으로 확정했다 — 장쭤린 폭살(1928)·만주사변(1931)·
러허작전(1933)·노구교사건(1937)·태원작전/산서성(1937, 이타가키가 또 나온다)·
노몬한사건(1939). 이시와라 간지·이타가키 세이시로가 1931년엔 같이 관동군에서
독단행동을 벌인 콤비였다가, 1937년엔 이시와라가 대본영에서 이타가키의 확전을
막으려다 밀려나 사직하는 게 사건을 잇는 실마리다.

일본 아시아역사자료센터(JACAR)를 처음 썼다 — 외무성 외교사료관 + 방위성
방위연구소 두 소장처를 함께 쓰는 첫 문서철이다. `JACAR_독단전행/pipeline/`에
`jacar_fetch.py`(브라우저 없이 curl로 PDF를 받는 법을 알아냈다 — 메타 페이지
말고 뷰어 페이지의 HTML 안에 PDF.js 경로가 박혀 있다)·`문서목록.json`(51건·
649쪽, 사건별로 사람이 고름)·`TRANSLATION_SPEC.md`·`에이전트_지시.md`를
갖춰 뒀고, 51건 전부 실제로 받아 쪽수까지 확인했다. **러허작전만 아직 문서를
못 골랐다** — 이 사건만 모은 자료군이 없어서(陸軍省大日記 계열 225건에서
추려야 함) 다음에 이어받으면 여기부터.

`pages/`(225MB, 649쪽)는 **압축 전이라 커밋 안 했다** — 로컬에만 있다. 이어받는
사람은 `python3 JACAR_독단전행/pipeline/jacar_fetch.py --list JACAR_독단전행/pipeline/문서목록.json`
로 다시 받은 뒤, 지면 줄이기(967c4ae 참고)부터 하고 번역 서브에이전트를 돌릴 것.

**2026.8.8**: 서브에이전트 19개(각 4~10건)를 돌려 남은 113건 전부와 장문 4건(X042·X065·
X119·X355)의 한국어를 채웠다. `W240` 하나만 `점검.py`가 ko/en 0.375로 걸리는데, 24항목짜리
명사 나열이라 한국어가 원래 더 짧아지는 자리임을 두 차례 재대조로 확인했다(`notes` 참고) —
잘린 것이 아니다. `make_docs.py`·`build.py`·`make_people.py`·`build_site.py` 로 열람기까지
다시 만들었다. **다음에 할 일의 1번은 끝났다.** 2~6번이 남았다.

## 이어받으면 맨 먼저

```bash
python3 CREST_전쟁/pipeline/점검.py --정리
```

**`--정리` 는 에이전트가 하나도 돌고 있지 않을 때만 붙여라.** 세션이 끊기면
`배정.json` 에 「맡겼다」고만 적힌 채 아무도 하지 않는 문서가 남는다.
`다음묶음.py` 는 맡긴 것을 다시 내주지 않으므로 풀어 주지 않으면 **그 문서는
영영 배정되지 않는다.** 반쪽 파일(ko 가 비었거나 중간에서 끊긴 것)도 같이
잡아낸다.

그 다음은 늘 같다.

```bash
python3 CREST_전쟁/pipeline/다음묶음.py 5     # 다섯 묶음을 내준다
# 묶음마다 서브에이전트 하나. 소넷. 지시서는 pipeline/에이전트_지시.md
```

## 다음에 할 일

**1. ~~1951년 번역을 끝낸다.~~ 2026.8.8 끝났다.** 822/823건, 장문 4건 포함. 남은
`W240` 하나는 잘린 게 아니라 원래 짧은 문서다(위 참고). 이제 2~6번이 남았다.

**2. 긴 문서 네 건.** `X042 X065 X119 X355`. 2만 자가 넘어 영문을 답변에
실어 나르다 검열에 걸리는 것들이라 **`en` 을 기계로 미리 채워 두었다**
(`mkskel.py`). 한국어만 쓰면 된다. 지시서가 따로 있다 —
`pipeline/에이전트_지시_긴것.md`. 보통 지시서를 주면 안 된다.

**3. 부대 번호·좌표를 지면으로 다시 훑어라. 특히 규모가 걸린 것.**
새 규칙이 서기 전에 만든 번역은 OCR 오독을 그대로 두고 지면 판독을
`notes` 에만 적어 둔 것이 있다. `에이전트_지시_지면반영.md` 가 그 일감이다.

    python3 -c "
    import json,glob,re
    for f in glob.glob('CREST_전쟁/tr/*.json'):
        n=json.load(open(f)).get('notes') or ''
        if re.search(r'지면',n) and re.search(r'\d',n): print(f)"

**X311 은 이미 재검증했다.** 1948년 4월 성진 파견 규모(10,000→140,000명 등
네 자리)를 지면반영이 고쳤길래 의심하고 `pages/X311-01~03.jpg` 를 직접 다시
읽었다 — 지면 판독이 맞았다. 다시 볼 필요 없다. (다섯 차례 파견분을 합치면
335,000명인데 서두의 「약 250,000명」과 어긋나는 것은 원문 자체의 불일치라
그대로 두었다.)

**4. 일일 회보 다섯 권.** 122만 자. 분기별로 제본한 것이라 낱개 문서가 아니다.
날짜별로 쪼갤지 따로 판단해야 한다.

**5. ~~PEARL_1941 의 빠진 지면 62장.~~ 2026.8.9 끝났다 — 진짜 없던 것은 9장뿐이었다.**
「62장」은 착각이었다. `raw/pages.json` 이 줄곧 **facs(스캔 파일 번호)** 를 담고
있었는데 `make_docs.py` 가 그걸 인쇄 쪽번호인 양 그대로 `docs.json` 에 넣고
있었다 — 두 체계가 숫자만 우연히 겹치는 자리가 93곳이라 화면은 깨지지 않고
'떴지만', 69건 문서 다수가 각주가 가리키는 것과 **다른 지면**을 보여주고
있었다. `frus1931-41v02.xml` 의 `<pb facs= n=>` 짝으로 `pages.json` 을 다시
지어 진짜 인쇄 쪽번호로 바꾸고, 파일 이름도 다른 문서철과 같은 규칙(0채움
없이)으로 되돌렸다. 그러고 나니 진짜 빠진 건 9장(150-152, 274-276, 796-798)
뿐이었고, TEI가 대괄호 쪽번호(`n="[150]"`)로 적어 둔 **인쇄 안 된 흰 면**이라
받아서 확인해 보니 실제로 백지였다. 155/155 스캔·썸네일 갖춰졌다.

**6. ~~남은 날짜 이상.~~ 2026.8.9 끝났다.** `X075`(1953) `X090`(1953) `X080`(1962)
`X141`(1956) `X214`·`X217`(일자까지 어긋남) 여섯 건 전부 지면을 열어 보니
색인과 맞았다 — OCR이 연도 끝자리나 일자를 잘못 읽었을 뿐이었다. `X217`은
한 걸음 더 나갔다: 예전에 뭉갠 OCR(`36 dune 1952`)을 보고 `16 June 1951`로
짐작해 고쳐 둔 적이 있었는데, 지면은 그것과도 다른 `18 June 1951`이었다 —
**짐작으로 맞춘 값도 다시 지면으로 확인해야 한다**는 사례로 남긴다.
`X156`은 날짜는 이미 맞았지만(`AUG 1978`은 정보 일자가 아니라 비밀해제
도장이었다) 지면에 또렷한 한자·생년월일·인명 다수를 판독 불가로 처리해
둔 것을 발견해 7쪽 전체를 다시 옮겼다.

**7. `[판독 불가]` 밀집 문서 재대조 — 진행 중, 상위 40건 끝났다.** raw OCR만
보고 지면을 충분히 대조하지 않은 채 「판독 불가」를 남발한 문서가 많다는 것이
드러났다. 밀도 상위 40건을 지면 전부 열어 다시 옮겼더니 대부분 지면 자체는
멀쩡했다 — X022(934→18), X119(268→4), X230(250→8), X177(78→2), X020(73→12,
덤으로 화교 인구 합계 오류 「3,686→34,686」도 잡았다) 등. 전체
**15,856곳 → 12,174곳**(2026.8.9 두 차례, 32건). X321(105→105)은 지면
자체의 물리적 인쇄 결함(줄마다 왼쪽 15~20자가 이중 인쇄로 뭉갬)이라 재대조로도
못 살렸다 — 정당한 판독 불가다. 남은 것은 문서당 밀도가 급격히 줄어(중간값
근처) 상당수가 진짜 삭제·훼손이거나 낱글자 한자·필기체 서명 수준이라 문서당
이득이 준다. 이어 하려면:

    python3 -c "
    import json,glob
    rows=[(json.load(open(f))['en'].count('판독 불가')+json.load(open(f))['ko'].count('판독 불가'), f)
          for f in glob.glob('CREST_전쟁/tr/*.json')]
    rows=[r for r in rows if r[0]>0]; rows.sort(reverse=True)
    for c,f in rows[:30]: print(c,f)"

## 스캔을 붙이는 법

**이 문서철만 스캔이 없다.** 저장소 원칙(README 「스캔을 같이 올린다」)과
어긋나고, 오른쪽 창이 **OCR 을 교정한 영문**이라 대조할 것이 더 필요한 자리다.

받을 수 있다. 실측한 것을 적어 둔다.

```
항목마다 Image Container PDF 가 있다        한 면에 40~110KB
대부분 한두 면짜리 첩보보고                309건 다 받아도 30MB 안팎
__ia_thumb.jpg (10KB) 도 항목마다 있다     썸네일로 쓸 수 있다
_site 724MB · GitHub Pages 한도 1,024MB    여유 300MB
```

**NARA 의 28MB 무압축 TIFF 와 혼동하지 마라. 전혀 다르다.**

두 가지를 조심해야 한다.

1. **식별자는 대문자다.** `index.json` 의 `crest` 는 이미 대문자로 고쳐 두었다
2. **메타데이터 API 가 자주 빈 `{}` 를 돌려준다.** HTTP 200 인데 내용이 없다.
   **초당 한 건 이하로, 빈 응답이 오면 한참 쉬었다 다시 붙어라**

받은 뒤 `collection.json` 에 `pages` 를 넣으면 열람기가 썸네일과 확대 뷰어를
살린다. 규격은 `열람기/SCHEMA.md`.

## 앞서 끝낸 것 — 전쟁을 보는 눈 (CREST_전쟁)

**377건.** 1950년 6월 26일에서 12월 30일까지. 개전 이튿날에서 시작한다.

묶음을 달이 아니라 **국면**으로 갈랐다. 이 반년은 전선이 세 번 뒤집히고
문서 성격이 그때마다 달라진다.

```
A  밀린다      6.26 – 8.31    포로 심문과 피난민 첩보가 는다
B  되민다      9.1 – 10.24    노획한 관보와 행정 자료가 는다
C  다시 밀린다  10.25 – 12.30  판단서와 계획서가 는다
```

### 모델은 소넷으로 간다

opus 는 한도를 너무 빨리 먹는다. 하이쿠는 **부대 번호를 고쳐서 못 쓴다** —
같은 문서(W220·W223)로 견줘 보니 `2hth Division` 과 `2th Division` 을 둘 다
`24th` 로 바꾸고 `past 2h hours` 도 24로 고쳤다. 두 표기가 같은 사단인지
모르는데 하나로 합친 셈이다.

소넷은 같은 자리에서 한 글자도 고치지 않고 「같은 사단을 가리키는지도
단정하지 않았다」고 적었다. **게다가 스스로 규칙 위반을 여섯 번 잡아내 되돌렸다** —
없는 지명을 지어냈다가, 사람 이름과 한자를 지어냈다가, `1980` 을 `1950` 으로
고쳤다가, `36th parallel` 을 38선으로 고쳤다가 모두 되돌리고 그 사실을 주에 적었다.

```python
Agent(subagent_type="general-purpose", model="sonnet", ...)
```

### 중복 배정을 막아라

물결로 나눠 돌리다 보면 **돌고 있는 것을 빼는 걸 잊는다.** 실제로 W049–W052 가
두 에이전트에 갔다. `pipeline/다음묶음.py` 가 끝난 것과 맡긴 것을 갈라 기록한다.

```bash
python3 CREST_전쟁/pipeline/다음묶음.py --남은것   # 얼마나 남았나
python3 CREST_전쟁/pipeline/다음묶음.py 4         # 네 묶음을 내주고 배정에 적는다
python3 CREST_전쟁/pipeline/다음묶음.py --풀기 W049 W050   # 실패한 것을 푼다
```

### 날짜는 색인을 따른다

노획한 북조선 관보를 옮긴 문서는 **관보의 발행일(1949년)** 이 본문에 있다.
그것을 `date` 에 넣으면 문서철 범위 밖으로 튄다. `date` 는 **중앙정보국이 그
보고를 낸 날**(색인)로 맞추고, 본문의 날짜는 원문 그대로 둔다. 실제로 열한 건을
그렇게 고쳤다.

## 앞서 끝낸 것 — CREST 1949–50

**155건.** 1949년 43건과 1950년 개전 전 112건이다.

### 왜 1950년 6월 25일에서 끊는가

이 문서철은 **미국이 조선을 밖에서 보는 기록**이다. 6월 25일부터 미국은 조선
안에 있다. CREST 1950년치 494건 가운데 개전 뒤가 380건 **180만 자**인데, 지금까지
만든 것을 전부 합친 것보다 많다. 섞으면 문서철이 전쟁 기록에 파묻힌다.

**「전쟁의 세 해」(FRUS 국무부 계통) 옆에 놓일 별개의 문서철**로 보고 남겨 두었다.
첫 건이 `_staging/CIA_1950_전시일보/` 에 번역까지 끝나 있다.

**날짜로 시기를 자르면 새어 들어온다.** 그 한 건이 메타데이터로는 6월 1일인데
본문이 개전 뒤였다. 무초가 수원에서 보고하고 트루먼의 지원 결정을 다룬다.
**날짜가 곧 편집 기준일 때는 본문을 열어 확인해야 한다.**

## 앞서 끝낸 것 — CREST 1945–48

**CIA 공개 기록(CREST)에서 고른 154건.** 네 계열이다.

```
L01–L07   1945 뉴욕 · OSS 「외국인 전문가 조사」
M01–M19   1946 만주·북조선 · 현장 첩보보고 (M04 는 뺐다. 아래 참조)
N01–N68   1947 북조선 · 중앙정보단·중앙정보국 보고
P01–P63   1948 두 정부 · 중앙정보국 보고 (P08·P51 은 사본 중복이라 뺐다)
```

**줄기가 하나라 한 문서철에 둔다.** 미국은 조선 안에 들어가지 못한 채 밖에서
읽는다. 1945년에는 뉴욕에서 사람을 만나, 1946년에는 만주에서 국경 너머로,
1947년에는 38선을 넘어온 사람에게서. 1948년에 두 정부가 서는데도 그대로다.

**주제(threads)에는 넣지 않았다.** 문서철로만 올렸다.

### 번역은 서브에이전트로 한다

**`pi` CLI 를 쓰지 않는다.** 앞선 문서철(`KOREA_1950_53`, `NDL_점령기자료`)의
`translate.py` 는 `pi` 를 부르는 껍데기인데, 이 문서철부터는 서브에이전트에
맡겼다. 자세한 이유는 `CREST_1945/pipeline/README.md`.

에이전트 하나에 문서 다섯에서 일곱 건을 주고 이렇게 시켰다. 넷을 나란히 돌려
스무 건에 열댓 분 걸렸다.

> `CREST_1945/pipeline/TRANSLATION_SPEC.md` 와
> `KOREA_1948_50/pipeline/TRANSLATION_SPEC.md` 와 `열람기/STYLE.md` 를 먼저 읽어라.
> `CREST_1945/raw/L01.txt` 를 읽고 `CREST_1945/tr/L01.json` 을 써라.
> 전문으로 옮기고, 줄이지 마라. OCR 을 교정한 영문을 `en` 에, 번역을 `ko` 에 넣어라.
> **문서번호와 날짜의 숫자는 고치지 마라.** `(b)(6)`·`25X1` 같은 삭제 표시를
> 지우지 말고 `redactions` 에도 모아라. 읽을 수 없으면 `[판독 불가]`.

**받은 것은 반드시 검증한다.** 실제로 걸린 것들이다.

- 긴 문서를 나눠 쓰다 만 이음표(`@@KO@@`)가 본문 끝에 남는다
- `ko` 가 `en` 의 절반에 못 미치면 잘렸거나 요약한 것이다. 열어 봐라
- **`ko` 칸에 영문이 그대로 들어간다.** 길이는 멀쩡해서 길이 검사로는 안 잡힌다.
  제목·요지는 한국어라 목록에서도 멀쩡해 보이고, **문서를 열어야 드러난다.**
  실제로 1,546건 가운데 한 건이 그랬다(`KOREA_1950_53/tr/50-d577.json`).

  ```python
  HAN = re.compile(r'[가-힣]')
  len(HAN.findall(ko)) / len(ko) < 0.15   # 이러면 열어 봐라
  ```

  좌표와 표가 많은 문서는 0.15 언저리로 낮게 나오니 바로 판정하지 말고 열어 볼 것.
- 스키마 16칸에 여분 칸이 붙거나 빠진다

```python
# raw 대비 en·ko 길이와 스키마를 한꺼번에 본다
python3 - <<'EOF'
import json,glob,os
SPEC=['doc_id','date','doc_type','from','to','classification','title_en','title_ko',
      'subject_ko','key_points_ko','en','ko','redactions','people','confidence','notes']
raw={os.path.basename(p)[:-4]:len(open(p).read()) for p in glob.glob('CREST_1945/raw/*.txt')}
for p in sorted(glob.glob('CREST_1945/tr/*.json')):
    d=json.load(open(p)); i=d['doc_id']; r=raw.get(i,0)
    en,ko=len(d.get('en') or ''),len(d.get('ko') or '')
    bad=[k for k in SPEC if k not in d]+[k for k in d if k not in SPEC]
    print(i,r,en,ko,'짧다' if en<r*0.6 else '', bad or '')
EOF
```

### 사본 중복을 먼저 걸러라

**CREST 는 부서마다 남은 사본을 따로 디지털화한다.** 그래서 한 간행물이 서로
다른 문서번호로 두 벌 올라와 있다. 1948년치에서 세 쌍이 나왔다. 날짜와 제목이
달라 보여도 같은 것일 수 있다 — `ORE 15-48` 은 메타데이터 날짜가 한 달 반
벌어져 있었다.

**옮기기 전에 걸러라.** 다 옮기고 나서 알면 그 품이 버려진다.

```python
def shingles(t, k=8):
    w = re.findall(r'[a-z]+', t.lower())
    return {' '.join(w[i:i+k]) for i in range(len(w) - k)}
len(A & B) / len(A)      # 0.7 을 넘으면 같은 문서로 보고 열어 봐라
```

사본마다 읽히는 데가 다르다. **더 온전한 쪽을 싣고 다른 쪽의 문서번호를
`notes` 에 적는다.**

### 긴 문서는 갈라 써라

**`N01`(2만 5천 자)에서 에이전트가 두 번 중단됐다.** 「Output blocked by content
filtering policy」다. 영문 전문을 통째로 다시 뱉는 대목에서 걸린다. 1만 자
아래는 걸리지 않았고 2만 자 위에서 걸렸다.

그래서 이 한 건만 **영문과 한국어를 갈라 만들었다.**

```python
# en 은 기계로. 줄 끝 하이픈으로 잘린 낱말을 잇고 문단을 되돌린다
import sys, re; sys.path.insert(0,'열람기'); from reflow import reflow
raw = open('CREST_1945/raw/N01.txt').read()
en = reflow(re.sub(r'(?<=[a-z])-[ \t]*\n[ \t]*(?=[a-z])', '', raw))
```

그렇게 `en` 을 채운 뼈대 JSON 을 먼저 만들고, 에이전트에게는 **한국어만** 맡겼다.
번역은 옮기는 일이라 걸리지 않는다. 손으로 교정하지 않았다는 사실은 그 문서의
`notes` 에 적어 두었다.

도구로 만들어 두었다 — `CREST_1945/pipeline/mkskel.py`.

    python3 CREST_1945/pipeline/mkskel.py P01 P12 P43

**13만 자를 넘으면 그마저 안 된다.** `P01`(SR-2, 영문 137,000자)은 영문을
조각으로 갈라 다섯 에이전트가 나눠 옮기고 이어 붙였다. 조각마다 같은 말을
쓰도록 **용어표를 적어 함께 주었다.** 말이 갈리면 한 문서가 아니게 된다.

이 방법은 **OCR 이 깨끗할 때만 쓴다.** `N01` 은 CIA 가 `RELEASE IN FULL` 로 낸
것이라 스캔이 좋았다. 뭉갠 문서에 쓰면 교정 안 된 영문이 그대로 올라간다.

### 날짜를 믿지 마라

**M04 를 뺐다.** 메타데이터가 `1946-05-10` 인데 문서 표제가
「WEEK OF 4 MAY - 10 MAY 1948」이었다. **월과 일은 맞고 연도만 두 해 어긋난다.**
번역은 끝나 있고 `_staging/CREST_1948_ORE/` 에 옮겨 두었다.

새 CREST 문서철을 지을 때는 **본문에서 연도를 뽑아 메타데이터와 맞춰 보고,
메타 연도가 본문에 한 번도 안 나오면 열어 본다.** 다만 정보보고의 `DIST.` 날짜가
이듬해인 것은 어긋난 것이 아니다 — 정보를 얻은 날과 배포한 날이 다르다.

**어긋난 것이 네 번이고 그 가운데 셋이 판단서(ORE)였다.** 현장 첩보보고는 메타데이터가 맞는데
완성된 분석문은 표지 날짜를 봐야 한다. `N44`(ORE 62)는 메타 10월 2일에 표지
**11월 18일**이라, 그대로 두었으면 11월 5일 유엔 총회 표결을 논하는 문서가
표결보다 앞선 것이 될 뻔했다. `ORE 15-48` 은 메타 2월 4일에 표지 3월 18일이다.
판단서는 표지가 있으니 반드시 열어 봐라.

넷째는 판단서가 아니라 **일보**였다. 메타 6월 1일에 본문이 개전 뒤다.
`_staging/CIA_1950_전시일보/` 에 있다. **날짜가 곧 편집 기준일 때는
연도만 맞춰 보고 넘기지 말고 본문을 열어야 한다.**

### 다시 지을 때

```bash
python3 CREST_1945/pipeline/make_docs.py
cd CREST_1945/reading && python3 ../../열람기/build.py collection.json reader.html && cd ../..
python3 열람기/make_people.py
python3 열람기/build_site.py site.json --out _site
```

---

## 그 밖에 남은 것

- 「38선을 넘을 것인가」·「이승만을 어떻게 할 것인가」 주제 — 1951–53년 문서가
  들어왔으니 이제 만들 수 있다. 기준은 `조사/README.md` 가 아니라 아래 원칙
- **JANIS 75** — 1945년 조선 정보 연구 386만 자. 넣을지 말지 정해야 한다
- **개전 뒤 CREST** — 380건 180만 자. 중앙정보국 계통의 전쟁 문서철이 따로 선다
- **`_staging/` 에 셋이 기다린다.** 전부 성격이 같다. 여러 나라를 한 장에 적은
  일보·주간 요약이라 조선 문서철에 넣으면 프랑스와 유고슬라비아가 따라온다.
  **일보만 따로 모으면 조선이 그날 몇 번째 항목이었는지가 드러난다**
  (`CIG_1946_일보/` 넷 · `CREST_1948_ORE/` 한 건 · `CIA_1950_전시일보/` 한 건)
- **`_site` 가 724MB 다.** GitHub Pages 산출물 한도가 1GB 다. 스캔을 PNG 에서
  JPEG 으로 바꾸면 크게 준다

## 주제에 문서를 고르는 기준

기계로 고르지 않는다. 시험은 하나다.

> **이 줄을 빼면 다음 줄이 이해되지 않는가.** 이해되면 뺀다. 중요해도 뺀다.

- **문장 시험** — 한 줄을 써 본다. 제목을 바꿔 적은 것이면 넣지 않는다
- **어긋남 우선** — 같은 것을 두 곳이 다르게 적은 자리는 무조건 넣는다
- **틀린 판단을 빼지 않는다** — 결과를 아는 쪽에서 골라내면 사후의 정답만 남는다
- 주제 하나는 15~30항. 넘치면 물음이 둘인 것이다
- 그날짜 문서가 없으면 **걸지 않는다.** 점선으로 둔다

## 조사 기록

`조사/README.md` 에 무엇이 어디 있고 어디가 막혔는지 적어 두었다.
**같은 곳을 두 번 뒤지지 않으려고 둔 것이다.** 새 세션은 그것부터 읽어라.

---

## 원본 지면을 쓸 수 있다 — 2026년 8월

전쟁 문서철에 스캔이 붙으면서 **번역 에이전트가 OCR 을 지면과 대조할 수
있게 됐다.** 이것이 판독 품질을 크게 바꾼다.

    CREST_전쟁/pages/X234-01.jpg   문서 번호에 면 번호를 붙인 이름이다

실제로 X233·X234 는 raw OCR 이 거의 해독 불가였는데 지면 석 장을 대조해
본문 대부분과 인명 한자(김규식 金奎植, 안재홍 安在鴻, 조완구 趙琬九,
엄항섭 嚴恒燮, 김약수 金若水 — 1950년 납북 정치인들이다)를 되살렸다.

**그래도 날짜와 숫자는 고치지 않는다.** 지면으로 확인했더라도 본문의 원문은
그대로 두고 `notes` 에 적는다. 이 규칙은 그대로다.
